# Global Claude Code Preferences

- Start new work in a worktree. Keep the main tree on main for review and merging.
- All GitHub repos are cloned into `~/git/`.

## Terminology
- "Ask codex" or "codex review" = use the OpenAI Codex MCP server (not a GitHub reviewer)
- When asked to "review" a PR, report findings back in conversation — do NOT post comments on the PR itself

This file is public (part of [dotfiles](https://github.com/dangerous/dotfiles)).
Private or sensitive instructions belong in `~/git/dotfiles-private/CLAUDE.md`.
